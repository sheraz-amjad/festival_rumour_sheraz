class AppDurations {
  static const Duration loadFestivalDelay = Duration(milliseconds: 100);
  static const Duration autoSlideInterval = Duration(seconds: 5);
  static const Duration slideAnimationDuration = Duration(milliseconds: 100);
  static const Duration otpAnimationDuration = Duration(milliseconds: 100);
  static const Duration shortDelay = Duration(milliseconds: 100);
  
  // 🔹 Loading State Durations
  static const Duration minimumLoadingDuration = Duration(milliseconds: 100);
  static const Duration buttonLoadingDuration = Duration(milliseconds: 100);
  static const Duration otpVerificationDuration = Duration(milliseconds: 100);
  static const Duration loginLoadingDuration = Duration(milliseconds: 100);
  static const Duration apiCallDuration = Duration(milliseconds: 100);
}
